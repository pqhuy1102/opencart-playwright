name: Playwright Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Start OpenCart
        run: docker compose -f opencart/docker-compose.yml up -d --build

      - name: Wait for OpenCart to be ready
        run: |
          echo "Waiting for OpenCart..."

          until curl --silent --fail http://localhost/ > /dev/null; do
            echo "OpenCart is not ready yet..."
            sleep 5
          done

          echo "OpenCart is ready!"

      - name: Seed test user
        run: |
          docker cp opencart/seed-test-user.php opencart-opencart-1:/tmp/seed-test-user.php
          docker exec opencart-opencart-1 php /tmp/seed-test-user.php

      - name: Run Playwright tests
        run: npx playwright test

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
